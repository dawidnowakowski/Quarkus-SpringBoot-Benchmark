info:
  name: Update Student
  type: http
  seq: 1

http:
  method: PUT
  url: http://localhost:8080/students
  body:
    type: json
    data: |-
      {
        "id": 1,
        "firstName": "Dawid",
        "lastName": "NowakowskiV2",
        "email": "aas@s"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

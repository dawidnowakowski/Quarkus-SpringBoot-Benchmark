info:
  name: Delete Student
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/students/:id
  params:
    - name: id
      value: "12"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

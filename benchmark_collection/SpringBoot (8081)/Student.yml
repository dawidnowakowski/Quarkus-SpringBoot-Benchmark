info:
  name: Student
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8081/students/:id
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

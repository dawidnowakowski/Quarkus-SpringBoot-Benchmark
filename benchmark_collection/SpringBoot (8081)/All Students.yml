info:
  name: All Students
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/students
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

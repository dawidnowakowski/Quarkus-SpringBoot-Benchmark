info:
  name: Create Student
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/students
  body:
    type: json
    data: |-
      {
        "firstName": "Dawid",
        "lastName": "NowakowskiV2",
        "email": "dawid.nowakowskiV2@student.put.poznan.pl"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Update Student
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/students
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
